var agrandarImagenes=function(event){

	$('.imagenParaAgrandar').on('click',function(event){

		if (window.matchMedia('(min-width: 450px)').matches)
		{
			var sr=$(this).attr('src'); 
			$('.imagenExpandida').attr('src',sr);
			$('.imagenExpandidaContenedor').css('display','inline');
			setTimeout(function(){
				$('.imagenExpandidaContenedor').css('visibility','visible');
				$('.imagenExpandidaContenedor').css('opacity','1');
			}, 200);
		}


	});

	

	$('.imagenExpandidaCerrar').on('click',function(event){

		$('.imagenExpandidaContenedor').css('opacity','0');
		$('.imagenExpandidaContenedor').css('visibility','hidden');
		setTimeout(function() {
			$('.imagenExpandidaContenedor').css('display','none');
			$('.imagenExpandida').attr('src','');
		}, 400);

	});

	$('.imagenExpandidaContenedor').on('click',function(event){

		$('.imagenExpandidaContenedor').css('opacity','0');
		$('.imagenExpandidaContenedor').css('visibility','hidden');
		setTimeout(function() {
			$('.imagenExpandidaContenedor').css('display','none');
			$('.imagenExpandida').attr('src','');
		}, 400);

	});

	$('.imagenExpandida').on('click',function(event){

		event.stopPropagation();

	});

	/* modal con dialog de jquery */
	/* 
	$( function(){
		$( "#dialog" ).dialog({
			resizable: false,
			height: "auto",
			width: 400,
			modal: true,
			autoOpen: false,
			show: {
			effect: "blind",
			duration: 1000
			},
			hide: {
			effect: "blind",
			duration: 200
			}
		});

		$( "#opener" ).on( "click", function() {
			$( "#dialog" ).dialog( "open" );
		});
	});
	*/ 

};

var agrandarImagenesVerificarVentana=function(event){
	if (window.matchMedia('(max-width: 450px)').matches)
		{
			$('.imagenExpandidaContenedor').css('opacity','0');
		$('.imagenExpandidaContenedor').css('visibility','hidden');
		setTimeout(function() {
			$('.imagenExpandidaContenedor').css('display','none');
			$('.imagenExpandida').attr('src','');
		}, 400);
		}
};

window.addEventListener('load', agrandarImagenes);
$( window ).resize(agrandarImagenesVerificarVentana);